# Whisper Benchmark Requirements
# ================================

# Core dependencies
faster-whisper>=0.10.0
ctranslate2>=3.0.0

# Memory monitoring
psutil>=5.9.0
pynvml>=11.0.0

# PyTorch (required by faster-whisper)
torch>=2.0.0
torchaudio>=2.0.0

# Optional: for audio conversion
# Install ffmpeg separately: apt-get install ffmpeg

# Optional: for whisper.cpp support
# Clone from: https://github.com/ggerganov/whisper.cpp
# Build with CUDA support if needed
